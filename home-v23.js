const content=window.APP_CONTENT;const tabButtons=[...document.querySelectorAll('[data-tab]')],panels=[...document.querySelectorAll('[data-panel]')];function esc(v){return String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;')}function activateTab(tab){tabButtons.forEach(b=>b.classList.toggle('active',b.dataset.tab===tab));panels.forEach(p=>p.classList.toggle('active',p.dataset.panel===tab));history.replaceState(null,'',`#${tab}`)}tabButtons.forEach(b=>b.addEventListener('click',()=>activateTab(b.dataset.tab)));function renderLessons(){const grid=document.querySelector('#lesson-grid');content.lessons.forEach(l=>{const a=document.createElement('a');a.className=`lesson-card ${l.status==='ready'?'ready':''}`;a.href=`lesson.html?id=${l.id}`;a.innerHTML=`<span class="lesson-number">${l.id}</span><span class="lesson-main"><strong>${esc(l.title)}</strong><small>${esc(l.group)}</small></span><span class="lesson-tags">${l.tags.map(t=>`<span>${esc(t)}</span>`).join('')}</span>`;grid.appendChild(a)})}function renderQuiz(){document.querySelector('#quiz-grid').innerHTML=content.quizCards.map(c=>`<a class="tool-card" href="${c.href}"><span class="tool-icon">?</span><span><strong>${esc(c.title)}</strong><small>${esc(c.description)}</small><em>${esc(c.badge)}</em></span></a>`).join('')}function renderVisualizers(){document.querySelector('#visualizer-grid').innerHTML=content.visualizers.map(c=>`<a class="tool-card" href="${c.href}"><span class="tool-icon">↕</span><span><strong>${esc(c.title)}</strong><small>${esc(c.description)}</small></span></a>`).join('')}renderLessons();renderQuiz();renderVisualizers();const initial=location.hash.replace('#','')||'lessons';activateTab(['lessons','quiz','visualizers','results'].includes(initial)?initial:'lessons');